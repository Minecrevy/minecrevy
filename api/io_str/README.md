# Minecrevy Structured IO

This crate provides high-level mechanisms to parse Minecraft protocol data types and packets in a structured fashion.

## Dependency

```toml
[dependencies]
minecrevy_io_str = "<version #>"
```
